# Placeholders

To set placeholders use `setPlaceholders` method from `storyManager`:

```ts
storyManager.setPlaceholders({ username: "John Doe" });
```

## Image Placeholders

```ts
storyManager.setImagesPlaceholders({
  image1: "https://example.com/image.jpg",
});
```
