---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---

# supportbot Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-07-07

## Active Technologies
- TypeScript (Node.js 20+) + Next.js (App Router), Tailwind CSS, UI kit (React Hero UI or equivalent) (002-ui-navigation)
- PostgreSQL (002-ui-navigation)
- TypeScript (Node.js 20+) + Next.js (App Router), Tailwind CSS + HeroUI, `pg` (003-broadcast-images)
- PostgreSQL (existing) (004-ui-polish)
- PostgreSQL (existing `broadcast_messages`, `delivery_results`, `broadcast_recipients`, `chats`) (005-chats-history-improvements)
- TypeScript (Node.js 20+) + Next.js (App Router), Telegram Bot API (`getUpdates`, `setWebhook`, updates JSON), `pg`; PostgreSQL `chats` + bot settings webhook UI (006-chat-management)
- TypeScript (Node.js 20+) + Next.js (App Router), React, `@heroui/react` (Card, Modal и др.), существующий `POST /api/broadcasts` (007-send-confirmation)
- N/A для фичи — без новых таблиц и миграций (007-send-confirmation)
- TypeScript (Node.js 20+) + Next.js (App Router), `@heroui/react`, Telegram Bot API (`sendVideo`, `sendMediaGroup` с видео), `pg` (008-broadcast-video)
- PostgreSQL — `broadcast_attachments` (расширение использования), `broadcast_messages` без обязательного изменения контракта контента (008-broadcast-video)
- TypeScript (Node.js 20+), Next.js 16 App Router + `pg`, `openai`, `zod`, `@heroui/react`, Telegram Bot API (`getUpdates`, long poll CLI), OpenAI API (GPT-4.1, text-embedding-3-small) (009-bot-qa-rag)
- PostgreSQL — `chat_messages` (+ `telegram_username`, `unanswered_reason`), `knowledge_chunks`, `knowledge_index_runs`, `processed_telegram_updates`; migrations 003–005; extension `vector` (009-bot-qa-rag)
- TypeScript (Node.js 20+), Next.js 16 App Router + GitHub Actions, `actions/checkout`, `actions/setup-node`, SSH deploy (`appleboy/ssh-action` или native ssh) (010-github-cicd)
- PostgreSQL + pgvector в CI (service container `pgvector/pgvector:pg16`); production DB на сервере (010-github-cicd)
- TypeScript (Node.js 20+), Next.js 16 App Router + `pg`, `zod`, `@heroui/react` (Modal), существующие `src/rag/*`, `src/telegram/processMessage.ts` (011-unanswered-context-metadata)
- PostgreSQL — новая таблица `unanswered_context_snapshots` (JSONB для чанков и диалога); FK на `chat_messages` (011-unanswered-context-metadata)
- TypeScript (Node.js 20+) + Next.js 16 App Router, React, `@heroui/react` (Modal), `pg`, `zod`, существующие `sendBroadcast`, `POST /api/broadcasts` (012-broadcast-edit-resend)
- PostgreSQL — `broadcast_messages`, `broadcast_recipients`, `broadcast_attachments`, `delivery_results` (без новых таблиц) (012-broadcast-edit-resend)
- TypeScript (Node.js 20+), Next.js 16 App Router + `pg`, `openai`, `zod`, `@heroui/react`, существующие `src/rag/*`, `POST /api/knowledge/reindex`, `BotSettingsForm` (013-bot-model-settings)
- PostgreSQL — расширение `bot_settings` (`answer_model`, `embedding_model`), `knowledge_index_runs.embedding_model`; migration `007`; `vector(1536)` без смены размерности (013-bot-model-settings)
- TypeScript (Node.js 20+), Next.js 16 App Router + существующие `pg`, `openai`, Telegram Bot API; без новых npm-пакетов (014-correct-links)
- PostgreSQL — `knowledge_chunks.metadata` (JSONB); `chat_messages.content` (Markdown) (014-correct-links)
- PostgreSQL — `chat_messages.content` (семантика: санитизированный Markdown для role=bot) (015-telegram-answer-formatting)

- TypeScript (Node.js LTS) + Next.js (App Router), Telegram Bot SDK, ORM/DB client, UI component primitives (001-telegram-broadcast-panel)

## Project Structure

```text
src/
tests/
```

## Commands

npm test && npm run lint

## Code Style

TypeScript (Node.js LTS): Follow standard conventions

## Recent Changes
- 015-telegram-answer-formatting: Added TypeScript (Node.js 20+), Next.js 16 App Router + существующие `pg`, `openai`, Telegram Bot API; без новых npm-пакетов
- 014-correct-links: Added TypeScript (Node.js 20+), Next.js 16 App Router + существующие `pg`, `openai`, Telegram Bot API; без новых npm-пакетов
- 013-bot-model-settings: Added TypeScript (Node.js 20+), Next.js 16 App Router + `pg`, `openai`, `zod`, `@heroui/react`, `src/rag/*`, `POST /api/knowledge/reindex`, `GET /api/knowledge/index-status`, bot settings UI


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
